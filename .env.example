# =============================================================================
# Energy Trading & Portfolio Optimization System
# Environment Variables Template
# =============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root directory
#    Command: cp .env.example .env (Linux/Mac) or copy .env.example .env (Windows)
#
# 2. Fill in your actual API keys and credentials below
#
# 3. NEVER commit .env to version control (already in .gitignore)
#
# 4. The application will read these variables using python-dotenv
#
# =============================================================================

# =============================================================================
# EIA API Configuration
# =============================================================================
# Get your free API key from: https://www.eia.gov/opendata/register.php
# Registration takes less than 2 minutes and provides instant access
# EIA API v2 provides electricity market data, generation, consumption, and prices

EIA_API_KEY=your_eia_api_key_here

# =============================================================================
# CAISO OASIS API Configuration
# =============================================================================
# CAISO OASIS is a public API - no authentication required
# Provides access to California ISO market data including LMPs, demand, and generation
# Some restricted reports may require certificates - not needed for this project

# No API key required for CAISO OASIS
# CAISO_CERT_PATH=/path/to/certificate.pem  # Optional: For restricted data

# =============================================================================
# Optional: Database Configuration (for future use)
# =============================================================================
# If you plan to use a database for data storage instead of Parquet files
# Uncomment and configure the appropriate database URL below

# PostgreSQL
# DATABASE_URL=postgresql://username:password@localhost:5432/energy_trading

# MySQL
# DATABASE_URL=mysql://username:password@localhost:3306/energy_trading

# SQLite (local file)
# DATABASE_URL=sqlite:///./energy_trading.db

# =============================================================================
# Optional: Logging Configuration
# =============================================================================
# Override default logging level from config.yaml
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

LOG_LEVEL=INFO

# =============================================================================
# Optional: Data Path Overrides
# =============================================================================
# Override default data storage paths from config.yaml
# Useful for storing data on external drives or network locations

# RAW_DATA_PATH=data/raw
# PROCESSED_DATA_PATH=data/processed
# MODEL_SAVE_PATH=models/saved

# =============================================================================
# Optional: API Rate Limiting
# =============================================================================
# Configure API request rate limits to avoid throttling

# EIA_REQUESTS_PER_HOUR=1000
# CAISO_REQUESTS_PER_MINUTE=60

# =============================================================================
# Optional: Feature Flags
# =============================================================================
# Enable/disable specific features

# ENABLE_CACHING=true
# ENABLE_MONITORING=false
# ENABLE_EMAIL_ALERTS=false

# =============================================================================
# Optional: Email Notifications (for alerts)
# =============================================================================
# Configure SMTP settings for email alerts about system status or errors

# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# ALERT_EMAIL=recipient@example.com

# =============================================================================
# Optional: Cloud Storage Integration
# =============================================================================
# For storing data and models in cloud storage (AWS S3, Azure Blob, GCP)

# AWS S3
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=energy-trading-data

# Azure Blob Storage
# AZURE_STORAGE_CONNECTION_STRING=your_connection_string
# AZURE_CONTAINER_NAME=energy-trading-data

# Google Cloud Storage
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
# GCS_BUCKET_NAME=energy-trading-data

# =============================================================================
# Optional: Monitoring & Analytics
# =============================================================================
# Integration with monitoring services (DataDog, New Relic, etc.)

# DATADOG_API_KEY=your_datadog_api_key
# NEW_RELIC_LICENSE_KEY=your_new_relic_key

# =============================================================================
# Development Settings
# =============================================================================
# Settings for development environment

# Set to 'development', 'staging', or 'production'
ENVIRONMENT=development

# Enable debug mode (more verbose logging, no caching)
# DEBUG=false

# =============================================================================
# Testing Settings
# =============================================================================
# Settings for running tests

# Use mock API responses instead of real API calls during testing
# USE_MOCK_API=true

# Path to test data
# TEST_DATA_PATH=tests/data

# =============================================================================
# End of Configuration
# =============================================================================
